大小比較手順。
	これは定型文である。
	これは「(左:整数)>(右:整数)」
	という構文である。
	マスクは整数である。
	マスクに数値「1」を代入する。
	機械「shlq $63,」マスク「」。
	機械「movq 」左「 , %rax」。
	機械「movq 」右「 , %rbx」。
	機械「subq %rax,%rbx」。
	機械「andq 」マスク「,%rbx」。
	機械「movq %rbx,%rax」。
確保する手順。
	これは整数である大きさを受け取る。
	これは整数を返す。
	個数は整数である。
	参照は整数である。
	個数に数値「0」を代入する。
	大きさ>個数のかぎり
	「
		機械「pushq $0」。
	」。
	機械「movq %rsp, 」参照「」。
	ここで参照を返す。
