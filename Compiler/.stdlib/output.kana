入力する手順。
	これは整数である大きさを受け取る。
	これは整数を返す。
	参照は整数である。
	参照に大きさを確保する。
	機械「movq $3 , %rax」。
	機械「movq $1 , %rbx」。
	機械「movq 」大きさ「 , %rdx」。
	機械「movq 」参照「 , %rcx」。
	機械「int $0x80」。
	機械「movq $mm , %rcx」。
	ここで参照を返す。
